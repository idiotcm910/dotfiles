# Do not hard-code a monitor name or resolution: this remains portable.
input type:keyboard {
    xkb_layout us
}

input type:touchpad {
    tap enabled
    natural_scroll enabled
    dwt enabled
}

output * bg #1a1b26 solid_color
