# Claude

Portable Claude Code configuration exported from `~/.claude`.

Tracked:

- `settings.json`
- custom agents
- locally maintained skills

Credentials, histories, projects, sessions, tasks, teams, shell snapshots,
logs, telemetry, downloads, backups and plugin caches are intentionally
excluded. Restore these files selectively into `~/.claude`; do not replace the
whole runtime directory.
