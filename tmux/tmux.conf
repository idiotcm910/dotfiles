# ~/thai/system/tmux/tmux.conf — cấu hình nền

# Prefix Ctrl-a (dễ bấm hơn Ctrl-b)
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Cơ bản
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -sg escape-time 0
set -g history-limit 50000
set -g focus-events on
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:Tc"   # true color, khớp nvim
setw -g mode-keys vi

# Reload config
bind r source-file ~/.tmux.conf \; display "Reloaded ~/.tmux.conf"

# Split giữ nguyên cwd
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# ─── Plugins (tpm) ───
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux#v0.3.0'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'

# continuum: tự lưu + tự khôi phục qua reboot
set -g @continuum-restore 'on'
set -g @continuum-save-interval '15'

# catppuccin
set -g @catppuccin_flavour 'mocha'

# yank: dùng wl-copy (Wayland)
set -g @yank_selection_mouse 'clipboard'

# sesh: popup xem tất cả workspace + nhảy
# -tzc: tmux session + zoxide (nhảy tới thư mục hay dùng) + configured
bind o run-shell "sesh connect \"$(sesh list -tzc -i | fzf-tmux -p 60%,55% --no-sort --ansi --prompt '  ' --border-label ' workspace ')\""

# Đồng bộ gõ trên mọi pane (chạy 1 lệnh nhiều pane)
bind e setw synchronize-panes \; display "sync-panes: #{?pane_synchronized,ON,OFF}"

# Gộp mọi window của session thành lưới pane; bấm lại để tách về như cũ (prefix z zoom một pane)
bind g run-shell '~/thai/system/bin/tmux-grid'

# Đóng: x = pane (mặc định) · X = cả window · Q = cả session
bind X confirm-before -p "Đóng window '#W'? (y/n)" kill-window
bind Q confirm-before -p "Đóng session '#S'? (y/n)" kill-session

# tpm PHẢI ở dòng cuối cùng
run '~/.tmux/plugins/tpm/tpm'
